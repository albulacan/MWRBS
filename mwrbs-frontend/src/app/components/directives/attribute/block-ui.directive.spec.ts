import { BlockUiDirective } from './block-ui.directive';

describe('BlockUiDirective', () => {
  it('should create an instance', () => {
    const directive = new BlockUiDirective();
    expect(directive).toBeTruthy();
  });
});
